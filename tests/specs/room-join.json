[
  {
    "name": "room join button prompts for link",
    "steps": [
      {
        "send": { "callback": "room:join" },
        "expect": [
          { "method": "answerCallbackQuery" },
          { "method": "sendMessage", "payload": { "text": "Paste the room invite link or the room code (6 letters/numbers):" } }
        ]
      },
      {
        "send": { "text": "ABCDEF" },
        "expect": [{ "method": "sendMessage" }]
      }
    ]
  }
]
